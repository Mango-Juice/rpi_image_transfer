/dts-v1/;
/plugin/;

/ {
    compatible = "brcm,bcm2711";

    fragment@0 {
        target-path = "/";
        __overlay__ {
            epaper_tx: epaper_tx_device {
                compatible = "epaper,gpio-tx";
                /* TX 파이에서 사용할 GPIO 핀들 */
                data-gpios = <&gpio 17 0>, <&gpio 18 0>, <&gpio 19 0>; // DATA0, DATA1, DATA2
                clock-gpios = <&gpio 20 0>; // CLOCK
                ack-gpios = <&gpio 21 0>;   // ACK (입력)
                status = "okay";
            };
        };
    };

    fragment@1 {
        target-path = "/";
        __overlay__ {
            epaper_rx: epaper_rx_device {
                compatible = "epaper,gpio-driver";
                /* RX 파이에서 사용할 GPIO 핀들 */
                rx-data-gpios = <&gpio 17 0>, <&gpio 18 0>, <&gpio 19 0>; // DATA0, DATA1, DATA2 (입력)
                rx-clock-gpios = <&gpio 20 0>; // CLOCK (입력)
                rx-ack-gpios = <&gpio 21 0>;   // ACK (출력)
                status = "okay";
            };
        };
    };
};